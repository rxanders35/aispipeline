[package]
name = "aispipeline"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = {version = "0.12"}
tokio = { version = "1", features = ["full"]}
openssl = { version = "0.10", features = ["vendored"] }